## 1. ภาพรวมและโครงสร้างโปรเจกต์ (Project Structure)

ระบบนี้พัฒนาด้วย **PHP** และ **MySQL** โดยแบ่งออกเป็น 2 ส่วนหลักคือ Frontend และ Backend API 

```
Web App/
│
├── index.html                         # หน้าเว็บหลัก (แสดงตารางสินค้า, ฟอร์มบันทึกข้อมูล และ Modal แจ้งเตือน)
│
├── css/
│   └── style.css                      # สไตล์ตกแต่งหน้าเว็บ (Custom CSS ร่วมกับ Bootstrap 5)
│
├── js/
│   ├── app.js                         # ควบคุมการทำงานหลัก (ดึงตารางสินค้า, ระบบค้นหา, ปุ่มแก้ไข และปุ่มลบ)
│   ├── form.js                        # จัดการฟอร์มสินค้า (ตรวจสอบความถูกต้อง Validation, ส่งข้อมูล และแจ้งเตือน Alert)
│   └── loaddata.js                    # ฟังก์ชันโหลดตัวเลือก Dropdown (หมวดหมู่ และ ผู้จัดจำหน่าย) จาก API
│
├── inc/
│   └── connDB.php                     # ฟังก์ชันเชื่อมต่อฐานข้อมูล MySQL ด้วย PDO (รองรับทั้ง Local และ Cloud)
│
└── api/
    ├── .htaccess                      # กำหนด Rewrite URL เพื่อส่งทุกคำขอมาประมวลผลที่ index.php
    ├── index.php                      # จุดรับคำขอหลักของ API (Entry Point) และลงทะเบียน Route ทั้งหมด
    │
    ├── core/
    │   ├── Router.php                 # ตัวจัดการเส้นทาง API (ตรวจสอบ Method และ Path เช่น /products/{id})
    │   └── Response.php               # คลาสจัดรูปแบบข้อมูลตอบกลับเป็น JSON มาตรฐานเดียวกันทั้งระบบ
    │
    └── controllers/
        ├── ProductController.php      # จัดการข้อมูลสินค้า CRUD (ค้นหา, เพิ่ม, ดึงข้อมูล, แก้ไข และลบ)
        ├── CategoryController.php     # ดึงข้อมูลหมวดหมู่สินค้าสำหรับ Dropdown (Read-only)
        └── SupplierController.php     # ดึงข้อมูลผู้จัดจำหน่ายสินค้าสำหรับ Dropdown (Read-only)
```

---

## 2. รายการเส้นทาง API (API Endpoints)

การติดต่อสื่อสารระหว่างหน้าเว็บกับฐานข้อมูลจะกระทำผ่าน RESTful API โดยส่งและรับข้อมูลในรูปแบบ **JSON** ผ่าน Endpoint ดังต่อไปนี้: 

| Method | Endpoint Path | หน้าที่การทำงาน | ข้อมูลที่ส่งกลับ |
|:---|:---|:---|:---|
| `GET` | `/api/products` | ดึงรายการสินค้าทั้งหมด (รองรับ Parameter `?search=คำค้น`) | รายการสินค้าแบบ JSON Array |
| `GET` | `/api/products/{id}` | ดึงข้อมูลสินค้าเฉพาะรายการตามรหัส ID | ข้อมูลสินค้าแบบ JSON Object |
| `POST` | `/api/products` | เพิ่มข้อมูลสินค้าใหม่เข้าระบบ | สถานะการทำงาน และ ID ของสินค้าใหม่ |
| `PUT` | `/api/products/{id}` | บันทึกการแก้ไขข้อมูลสินค้าตามรหัส ID | สถานะการอัปเดตข้อมูล |
| `DELETE` | `/api/products/{id}` | ลบข้อมูลสินค้าออกจากระบบตามรหัส ID | สถานะการลบข้อมูล |
| `GET` | `/api/categories` | ดึงรายชื่อหมวดหมู่สินค้าทั้งหมดสำหรับ Dropdown | รายชื่อหมวดหมู่แบบ JSON Array |
| `GET` | `/api/suppliers` | ดึงรายชื่อผู้จัดจำหน่ายทั้งหมดสำหรับ Dropdown | รายชื่อผู้จัดจำหน่ายแบบ JSON Array |

---

## 3. ขั้นตอนการทำงานของระบบ (System Workflow)

### 3.1 การรับส่งข้อมูลผ่าน API 
ทุกคำขอจากฝั่งหน้าเว็บจะมีลำดับการประมวลผลดังนี้
1. เมื่อหน้าเว็บส่งคำขอมาที่เส้นทาง `/api/...` ไฟล์ `.htaccess` จะดักจับแล้วส่งต่อ URL ไปให้ `api/index.php`
2. `api/index.php` จะเรียกใช้การเชื่อมต่อฐานข้อมูลจาก `inc/connDB.php` และส่งต่อ URL พร้อม Method (GET, POST, PUT, DELETE) ให้กับคลาส `Router.php`
3. `Router.php` ตรวจสอบเงื่อนไขแล้วส่งต่อไปยัง Controller ที่เกี่ยวข้อง (`ProductController`, `CategoryController`, หรือ `SupplierController`)
4. Controller ดำเนินการ Query หรือปรับปรุงข้อมูลในตาราง MySQL (`tb_products`, `tb_categories`, `tb_suppliers`)
5. ผลลัพธ์ทั้งหมดจะถูกส่งกลับไปให้หน้าเว็บในรูปแบบ **JSON** ผ่านคลาส `Response.php` พร้อม HTTP Status Code ที่เหมาะสม (เช่น 200 OK, 201 Created, 404 Not Found, 422 Unprocessable Entity)

---

### 3.2 การโหลดหน้าเว็บครั้งแรก 
1. เมื่อผู้ใช้เปิดหน้าเว็บ `index.html` ผ่านเบราว์เซอร์
2. สคริปต์ `loaddata.js` จะส่งคำขอแบบ Asynchronous ในทันที:
   - ส่ง `GET /api/categories` เพื่อนำข้อมูลหมวดหมู่มาใส่ในช่องเลือกหมวดหมู่ (`<select id="categorySelect">`)
   - ส่ง `GET /api/suppliers` เพื่อนำข้อมูลผู้จัดจำหน่ายมาใส่ในช่องเลือกผู้จัดจำหน่าย (`<select id="supplierSelect">`)
3. ในเวลาเดียวกัน สคริปต์ `app.js` จะส่งคำขอ `GET /api/products` เพื่อดึงข้อมูลสินค้าทั้งหมด
4. เมื่อได้รับข้อมูล JSON หน้าเว็บจะนำข้อมูลมาแสดงผลลงในตารางรายการสินค้า พร้อมแสดงชื่อหมวดหมู่และชื่อผู้จัดจำหน่ายให้ครบถ้วน

---

### 3.3 การค้นหาข้อมูลสินค้า 
1. ผู้ใช้พิมพ์คำค้นลงในช่องค้นหา (สามารถค้นหาได้ทั้งจากชื่อสินค้า, ชื่อหมวดหมู่ หรือชื่อผู้จัดจำหน่าย)
2. หน้าเว็บส่งคำขอ `GET /api/products?search=คำค้น` ไปยังหลังบ้าน
3. `ProductController` จะค้นหาข้อมูลในฐานข้อมูลด้วยคำสั่ง `LIKE` ผ่าน Prepared Statement
4. เซิร์ฟเวอร์ส่งผลลัพธ์รายการสินค้าที่ตรงเงื่อนไขกลับมา และหน้าเว็บจะอัปเดตแสดงผลในตารางทันที

---

### 3.4 การเพิ่มข้อมูลสินค้าใหม่ 
1. ผู้ใช้กรอกรายละเอียดสินค้าลงในฟอร์ม (ชื่อสินค้า, หมวดหมู่, ผู้จัดจำหน่าย, หน่วยนับ, ราคา) แล้วกดปุ่ม **"บันทึกข้อมูลสินค้า"**
2. **การตรวจสอบข้อมูลฝั่งหน้าเว็บ (Client-side Validation):** 
   - สคริปต์ `form.js` ตรวจสอบความครบถ้วนของข้อมูล
   - ชื่อสินค้าต้องไม่เป็นค่าว่าง และความยาวไม่เกิน 30 ตัวอักษร
   - ต้องเลือกหมวดหมู่และผู้จัดจำหน่าย
   - ราคาต้องเป็นตัวเลขและมีค่ามากกว่าหรือเท่ากับ 0
   - หากข้อมูลไม่ถูกต้อง ระบบจะแสดงกรอบสีแดงและข้อความแจ้งเตือนใต้ช่องที่ต้องแก้ไขทันที พร้อมทั้งระงับการส่งข้อมูล
3. หากข้อมูลถูกต้องครบถ้วน สคริปต์จะแปลงข้อมูลเป็น JSON แล้วส่งไปยังหลังบ้านด้วยคำขอ `POST /api/products`
4. **การตรวจสอบข้อมูลฝั่งเซิร์ฟเวอร์ (Server-side Validation):**
   - `ProductController` ตรวจสอบความถูกต้องของข้อมูลซ้ำอีกครั้งเพื่อความปลอดภัย
   - บันทึกข้อมูลเข้าสู่ตาราง `tb_products` ด้วยคำสั่ง `INSERT INTO ...` โดยใช้ PDO Prepared Statement เพื่อป้องกันปัญหา SQL Injection
5. เมื่อบันทึกสำเร็จ เซิร์ฟเวอร์จะตอบกลับด้วยรหัส HTTP 201
6. หน้าเว็บจะแสดง Modal แจ้งเตือนว่า **"บันทึกข้อมูลสินค้าสำเร็จ!"** พร้อมรายละเอียดสินค้าที่เพิ่ม ล้างข้อมูลในฟอร์ม และโหลดข้อมูลสินค้าในตารางใหม่ทันที

---

### 3.5 การแก้ไขข้อมูลสินค้า 
1. ผู้ใช้กดปุ่ม **แก้ไข** (ไอคอนรูปดินสอ) ที่แถวสินค้าที่ต้องการแก้ไขในตาราง
2. สคริปต์ `app.js` จะดึงข้อมูลของสินค้ารายการนั้นมาใส่ลงในช่องต่างๆ ของฟอร์มโดยอัตโนมัติ
3. ระบบจะเปลี่ยนสถานะของฟอร์มเป็น **โหมดแก้ไข (Update Mode)** โดยแสดงแถบแจ้งเตือนสีเหลืองระบุรหัสสินค้าที่กำลังแก้ไข และเปลี่ยนข้อความบนปุ่มเป็น "บันทึกการแก้ไข"
4. หน้าจอจะเลื่อนลงมายังตำแหน่งของฟอร์มโดยอัตโนมัติ
5. เมื่อผู้ใช้แก้ไขข้อมูลเสร็จสิ้นแล้วกดปุ่มบันทึก สคริปต์ `form.js` จะตรวจสอบความถูกต้องของข้อมูลตามเงื่อนไข
6. ส่งข้อมูลที่แก้ไขแล้วไปยังหลังบ้านด้วยคำขอ `PUT /api/products/{id}`
7. `ProductController` ทำการอัปเดตข้อมูลในฐานข้อมูลด้วยคำสั่ง `UPDATE tb_products SET ... WHERE i_ProductID = ?`
8. เมื่ออัปเดตสำเร็จ หน้าเว็บจะแสดง Modal แจ้งเตือนว่า **"แก้ไขข้อมูลสำเร็จ!"** รีเซ็ตฟอร์มกลับสู่โหมดเพิ่มสินค้าปกติ และอัปเดตข้อมูลในตารางสินค้าให้เป็นข้อมูลล่าสุดทันที

---

### 3.6 การลบข้อมูลสินค้า
1. ผู้ใช้กดปุ่ม **ลบ** (ไอคอนรูปถังขยะ) ที่แถวสินค้าที่ต้องการลบในตาราง
2. เพื่อป้องกันความผิดพลาดจากการกดโดยไม่ได้ตั้งใจ ระบบจะแสดง **Modal ยืนยันการลบ (Confirm Delete Modal)** ขึ้นมาถามผู้ใช้ก่อนเสมอ
3. เมื่อผู้ใช้กดยืนยันการลบ สคริปต์ `app.js` จะส่งคำขอ `DELETE /api/products/{id}` ไปยังหลังบ้าน
4. `ProductController` ทำการลบข้อมูลออกจากตารางด้วยคำสั่ง `DELETE FROM tb_products WHERE i_ProductID = ?`
5. เมื่อลบสำเร็จ เซิร์ฟเวอร์ส่งการยืนยันกลับมา
6. หน้าเว็บจะปิด Modal ยืนยัน แสดงข้อความแจ้งเตือนว่าลบข้อมูลสำเร็จ และนำแถวสินค้านั้นออกจากตารางทันทีโดยไม่ต้องรีเฟรชหน้าเว็บทั้งหน้า
